<?php

// Uncomment this section to enable the shop

/*
if(isset($text)){
    
    sendMessage($chat_id, 
        getTexts("SHOP_TXT", $lang),
        composeKeyboard("SHOP", $lang)
    );

}

if(isset($data)){

    if(str_starts_with($data, "shop:")){

        // Set here the sections of the shop

    } else{
        
        editMessage($chat_id, $message_id,
            getTexts("SHOP_TXT", $lang),
            composeKeyboard("SHOP", $lang)
        );

        answerCall($callbackId,
            getTexts("SHOP_CALLBACK", $lang)
        );
        
    }
    
}
*/

?>